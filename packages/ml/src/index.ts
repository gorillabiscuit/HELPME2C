// Phase 2 bootstrap placeholder. Removed when the first real export
// (recommendForUser, scoreCompatibility, etc) lands per packages/ml/CLAUDE.md.
export function hello(): string {
  return 'packages/ml online';
}
